#ifndef VARIANT_FUNC_H
#define VARIANT_FUNC_H
#include "arr_func.hpp"

#include <cstddef>

namespace zubarev
{
  int getCouOfColNoIden(const int* matrix, size_t rows, size_t cols);
  int getMaxSumInDia(const int* matrix, size_t rows, size_t cols);
}
#endif
